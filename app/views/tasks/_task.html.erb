<li id="task-<%= task.id %>">
  <div class="container-fluid tasksInfo">
    <div class="row">
      <div class="col-xs-6 chkbox">
        <p class="chkTxt"><%= check_box_tag '', '', task.do, {'data-id' => task.id, 'data-user_id' => task.user_id} %>←タスクが完了したらココをチェック!</p>
      </div>
      <div class="col-xs-6 timeDisp">
        残り<span class="taskTime"><%= task.tasktime %></span>時間
      </div>
    </div>
  </div>
  
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title"><%= task.title %></h4>
    </div>
    <div class="panel-body">
      <span class="content"><%= task.content %></span>
    </div>
    <% if current_user == task.user %>
      <p class="delP"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span><%= link_to "削除", task, method: :delete, data: { confirm: "削除して宜しいですか?" } %></p>
    <% end %>
  </div>
</li>